service: lambda-firebase-update-customer

provider:
  name: aws
  runtime: nodejs6.10
  profile: etherstellar

  environment:
    stripe_secret_key: ...
    stripe_publishable_key: ...
    firebase_endpoint: ...
    firebase_token: ...

functions:
  updateFromCustomerUpdate:
    handler: handler.updateFromCustomerUpdate
    events:
      - http: post /

  updateFromSubscriptionUpdate:
    handler: handler.updateFromSubscriptionUpdate
    events:
      - http: post /
